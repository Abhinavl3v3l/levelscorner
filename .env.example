# Overrides values in backend/config/dev.yaml or production.yaml
# Copy to .env and fill in real values

# JWT secret — required in production, dev.yaml has a placeholder
JWT_SECRET=change_me_in_production
